// Os colchetes são a notação para arrays
// as arrays aceitam qualquer tipo de dados

var arry = [5, 'Guilbert', true, {teste: 1}]
console.log(arry)

// Podemos acessar elementos de arrays da seguinte forma:

console.log(arry[2])

// Podemos adicionar elementos no array

arry[4] = 'NOVO ELEMENTO'

console.log(arry[4])

// Podemos reescrever elementos

arry[0] = 'Elemento reescrito'
console.log(arry[0])